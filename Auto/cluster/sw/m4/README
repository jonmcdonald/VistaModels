
You must have vista in your PATH, to check this type this:

$ which vista

You also need the bare metal ARM cross compiler installed, to confirm type:

$ arm-none-eabi-gcc --version


Then, to build simply type:

$ make clean
$ make


Once complete you should expect to find this file:

image.axf


